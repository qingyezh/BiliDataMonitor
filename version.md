# 版本记录

## v0.2.2.9 (2026-09-20)

- feat: 对比列表列距与操作按钮
  - 指标/24h 子列固定窄宽并压缩单元格 padding
  - 操作改为水平「详情 / 移除」，plain 边框底纹样式

## v0.2.2.8 (2026-09-18)

- feat: 对比列表多级表头
  - 「指标」「24h增量」各自展开三列子表头（按首个目标类型：播放/弹幕/评论 或 点赞/评论/…）
  - 子列与表头对齐，数字右对齐 tabular-nums

## v0.2.2.7 (2026-09-18)

- feat: 对比列表指标与24h增量三项 + 数字右对齐
  - 指标：视频/UP 显示 播放/弹幕/评论；动态 点赞/评论/转发；专栏 点赞/评论/收藏
  - 24h增量：同上三项分别计算末点−距末点24h最近快照，正绿负红
  - 单元格内标签在左、数字右对齐（tabular-nums）

## v0.2.2.6 (2026-09-18)

- fix: 对比页工具栏顺序与日期宽度
  - 「标度下拉」与「原始/增量」对调：指标 | 标度 | 原始/增量 | 日历/T+0 | 间隔 | 时间
  - 日历选择器外包固定 220px 容器 + `!important`，避免 Element 默认宽度顶回
  - 根因：线上 index.html 曾指向旧 Compare 资源，导致改样式不生效；本次连同入口一并更新

## v0.2.2.5 (2026-09-18)

- feat: 对比页工具栏微调
  - 左侧功能组之间用竖线分隔（指标 | 原始/增量 | 标度 | 日历/T+0 | 间隔 | 时间）
  - 绝对值/增长率/指数100 改为下拉选择框
  - 日历时间选择器总宽收窄为 220px，输入区更紧凑

## v0.2.2.4 (2026-09-18)

- feat: 对比页布局重排
  - 标题行只保留「多目标对比」
  - 第二行：指标/模式/标度/时间轴/间隔 + 时间选择器 + 刷新（右对齐）
  - 目标区合并为一块列表：表头「+ 列表/清空」，操作列「详情/移除」，去掉 chips 行
  - 图表左下：对数/均值/趋势/叠加分P + 说明；右下：CSV/PNG

## v0.2.2.3 (2026-09-18)

- feat: 对比页工具栏设置本地缓存
  - 指标/模式/时间轴/间隔/开关/T+0窗/日期范围等写入 localStorage（`bili_compare_ui_v1`）
  - 按登录 username 分桶；刷新后自动恢复，清浏览器缓存才会重置
  - URL 查询参数仍优先（分享链接可覆盖缓存）

## v0.2.2.2 (2026-09-18)

- feat: 对比页目标区改为表格列表
  - 原卡片网格改为 el-table：类型 / 名称 / 别名 / 指标 / 24h增量 / 操作
  - 别名设置、修改、清除仍在表内完成，行为与图例联动不变

## v0.2.2.1 (2026-09-17)

- feat: 对比页工具栏三行 + 条目别名
  - 时间选择器与对比目标列表各自独立成行
  - 对比卡支持设置/修改/清除别名（上限 20 字），图例与 chips 优先显示别名
  - 别名存 localStorage（`bili_compare_alias_v1`），按登录 username 分桶，不跨账号
  - 悬停图例显示「别名（原标题）」；清除后回退自动短名

## v0.2.2.0 (2026-09-17)

- feat/fix: 对比页 T+0 相对网格 + 相对时间窗
  - 根因：1h+ 断裂来自「绝对墙钟重采样 → 相对分桶」双网格错位 + 增量 gap null
  - 新增 `resampleRelativeSeries`：以各序列首点为 T0，在相对时间上分桶 + LOCF 稠密化
  - 真断档阈值 `interval×3` 才保留 null；增量 `buildDeltaOnRelative` 在稠密序列上计算
  - T+0 时间选择器改为相对窗：零点=各自开始记录，`T+a~T+b` 小时/天
  - 快捷：前24h / 前48h / 前7天 / 全部；默认窗 48h，点数 N=窗长/间隔（5min→576，6h→8）
  - 曲线起点可为 T+a（不必是历史最开始）；选完相对窗即自动对齐

## v0.2.1.9 (2026-09-17)

- fix: 对比页 T+0 重合区曲线断成碎点
  - 根因：相对时间仍用 20s 槽对齐，各序列采样相位不同导致大量 null
  - 改为 `alignByRelativeBuckets`：按所选重采样间隔对相对时间分桶，同桶取末次值
  - 起点对齐语义不变，重合区曲线应连续

## v0.2.1.8 (2026-09-16)

- feat: 对比页 T+0 改为起点对齐
  - 各序列以自身首点为 T0，用相对时间做时间槽对齐（不再按全局最早点换算标签）
  - 起点较晚的序列不再在左侧空一段，曲线头部对齐
  - 底部说明增加「T+0：各序列起点对齐」

## v0.2.1.7 (2026-09-17)

- feat: 近重复清理「按推荐全选」
  - 一键勾选所有建议删除（累计指标较小侧）；可清空选择后再手动调整

## v0.2.1.6 (2026-09-17)

- fix: 对比页图例重复
  - 图例 key 改为系列下标；短名撞车时附加目标尾缀，保证系列名唯一

## v0.2.1.5 (2026-09-17)

- fix: 图例每行最多 4 项，超出换行，避免横向溢出

## v0.2.1.4 (2026-09-17)

- feat: 对比页「叠加分P」
  - 非「分P数」指标时可开关，在主指标上叠加各视频分P（图例「分P·…」，第三轴）
  - 或将指标切到「分P数」单独对比分P

## v0.2.1.3 (2026-09-17)

- fix: 详情页去掉多余的左下角「分P」开关（图例仍可点开分P）
- fix: 详情路由改为响应式 type/target + 就地刷新，去掉整页 reload
- fix: 对比页 URL 同步改用 query 对象，避免 hash 路由异常
- feat: 对比页保留「分P数」指标（仅视频目标）

## v0.2.1.2 (2026-09-17)

- feat: 对比页支持「分P数」指标
  - 语义指标增加 `page`（video: `page_count`）；增量模式即分P增加量
  - 非视频目标在该指标下自动不画线；页脚说明「分P仅视频目标参与对比」

## v0.2.1.1 (2026-09-17)

- feat/fix: 对比页与分P曲线体验
  - 对比页增加时间范围筛选（全部/收起），图例短名 + 悬停全名
  - 对比默认指标改为播放/流量；对齐前接入间隔重采样
  - 分P曲线：独立第三 Y 轴（默认隐藏），左下角「分P」开关；增量图例名「分P增加量」
- chore: 每次功能改动同步 git commit + version.md / AGENTS.md

## v0.2.1.0 (2026-09-17)

- feat: 多目标对比页 `/compare`
  - 跨类型 UP/视频/动态/专栏；语义指标映射；绝对值/增长率/指数100
  - 日历时间 / 相对 T+0 轴；20s 槽对齐；与详情一致的间隔重采样、对数、均值、趋势、导出
  - 入口：列表勾选、详情「加入对比」（导出CSV左侧）、UP 视频表勾选
  - 迷你卡：指标摘要 + 24h + 打开详情
- feat: 近重复数据清理
  - 设置页扫描间隔 <4s 的双实例脏点，两点并列展示，手动勾选删除
  - 后端 `GET /api/monitor/near-duplicates` + `deleteHistoryPoint`
  - 画图前端统一 `dedupeNearDuplicates`（≤4s 累计取 max）
- 详情/列表对比集 sessionStorage + URL 持久化

## v0.2.0.7 (2026-09-17)

- fix: 间隔选择改为时间桶重采样
  - 不再要求严格等间隔；按 floor(ts/interval) 分桶，每桶取末次快照
  - 5分钟/1h/6h/24h 均作用于原始值、增量、评论下降曲线
  - 增量断线阈值放宽为 间隔×2.5（容许抖动与偶发缺样）

## v0.2.0.6 (2026-09-17)

- fix: 曲线空白/不刷新
  - 趋势线配置变更不再 dispose 图表（原先内联数组每次渲染触发重建导致空白）
  - Detail 使用稳定 `TREND_LINE_SERIES` 引用
  - LineChart 增加 ResizeObserver，数据更新后 resize

## v0.2.0.5 (2026-09-17)

- feat: 图表左下角相邻点间隔选择
  - 对数开关左侧增加选择框：5分钟 / 1小时 / 6小时 / 24小时
  - 增量模式下相邻快照时间差超过「所选间隔×1.5」时该点断线（null）
  - 默认按轮询间隔设置映射到最近档位；四类详情图表共用

## v0.2.0.4 (2026-09-17)

- feat: 单实例防护
  - 启动获取 `app/data/bili-monitor.lock` PID 锁，已有存活实例则拒绝启动
  - 端口被占直接失败，不再自动顺延端口（避免双实例共用 SQLite）
  - 新增 `scripts/instance-guard.mjs`：扫描多余实例并终止（保留锁内 PID）
  - start.sh / stop.sh 接入守护与锁清理

## v0.2.0.3 (2026-09-17)

- chore: 隐藏曲线图删除功能常驻文字提示
  - 移除 LineChart 图下「悬停…按 L」提示条及样式
  - 移除 Detail 四类图表脚注中的 L 键说明
  - 交互与二次确认逻辑不变；文档补充删除 API 与实现约定

## v0.2.0.2 (2026-09-08)

- feat: 曲线图异常数据点删除
  - 悬停数据点后按 `L` 锁定该点，弹出二次确认框后删除整条历史快照
  - 支持 UP / 视频 / 动态 / 专栏 四类详情图表；原始值与增量模式均可
  - 后端 `DELETE /api/monitor/history/:kind/:id`（root），删除后视频自动重算 `video_metrics`
  - 数据库与曲线同步刷新；确认框展示时间与指标摘要

## v0.2.0.1 (2026-08-31)

- feat: 图表均值曲线
  - LineChart 新增 `showAvgLine`，对可见系列画水平均值虚线（markLine）
  - 对数模式先在原始值求均值再映射坐标；原始值/增量/下降模式均可用
  - UP/视频/动态/专栏详情页右上角增加「均值」开关（默认关闭，在「标签」前）
- feat: 概览区近24小时增量
  - 末点 − 距「末点−24h」最近的快照
  - UP/视频：播放/弹幕/评论；动态/专栏：点赞/评论/转发或收藏
  - 正增量绿、负增量红；历史不足24h 标注「不足24h」；无数据显示 `--`

## v0.2.0.0 (2026-08-31)

- feat: Linux (Ubuntu 24) 适配
  - Rust 托盘：libayatana-appindicator 替代 winapi，curl 替代 powershell，xdg-open 替代 cmd
  - 自启动：systemd user service 替代 Windows 注册表 + VBS
  - 启停脚本：Shell 脚本替代 BAT 批处理
  - 前端：自启动文字改为中性"开机自启动"
  - 后端：浏览器打开支持 xdg-open (Linux) / open (macOS) / start (Windows)

## v0.1.1.0 (2026-08-18)

- feat: 日志压缩归档功能
  - 使用 zlib 压缩旧日志为 .gz 格式
  - 启动时 + 跨天自动触发归档
  - 归档到 app/logs/archive/ 目录
- feat: Esc 键自动返回上一页
  - 全局 keydown 监听，排除输入框和弹窗
- feat: 图表数据点默认限制
  - 默认显示最新 200 个数据点防止卡顿
  - 手动选择日期范围时显示全部数据
- fix: 数据库损坏恢复
  - 从备份数据库恢复数据
  - 从 video_history 重建 mid=1340190821 的 up_history
  - 重建所有派生缓存表
- chore: 清理临时文件和旧备份

## v0.1.0.0 (2026-08-12)

- feat: BiliDataMonitor 独立项目（从 CrawlerAnalysis UP主分析功能独立）
  - 技术栈：Node.js + TypeScript 后端（Fastify + node:sqlite + axios + 自实现 WBI 签名）、Vue3 前端、Rust 托盘，零 Python 依赖
  - 监测维度：UP主（可限定最新 N 个视频）+ BV号单视频
  - 间隔轮询调度（5分钟~24小时可调，动态生效，系统启动即运行）
  - SQLite 双表分离存储（videos 当前值 + video_history 历史）+ 派生指标缓存表（up_metrics/up_daily_stats/up_monthly_trend/up_duration_dist/video_metrics）
  - 快照去重（四项指标全等跳过）+ 90 天自动清理
  - 系统集成：Windows 开机自启动 + Rust 系统托盘 + 自动打开浏览器
  - Cookie 管理界面与 CrawlerAnalysis 同款（掩码前4+***+后4，覆盖/取消/保存）
  - 端口 8123（127.0.0.1 回环，settings.json 可配置）
  - 目录结构：src/ 全部源码 + app/ 运行产物（web/config/data/logs/tray.exe）
